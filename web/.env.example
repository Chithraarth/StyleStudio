# Base URL path the app is served under. Use "/" for a standalone deploy.
BASE_PATH=/

# Port for the local dev server
PORT=5173

# No auth provider is wired up right now (Clerk was removed; Firebase is a
# planned follow-up) — every route is reachable directly.
